import React, {useState} from 'react';
import {HamburgerIcon, CloseIcon} from '@chakra-ui/icons';
import {
    Box,
    Button,
    Drawer,
    DrawerOverlay,
    DrawerContent,
    DrawerCloseButton,
    DrawerHeader,
    DrawerBody
} from '@chakra-ui/react';

interface HamburgerMenuProps {
}

export const Page3: React.FC<HamburgerMenuProps> = ({}) => {
    return (
        <Box>
            {/*<Button id="complete" color="#61dafb" top="110px" width="100px">complete</Button>*/}
        </Box>
    );
};
